import 'package:flutter/material.dart';

class BookmakerWidget extends StatelessWidget {
  const BookmakerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bookmaker Screen');
  }
}
