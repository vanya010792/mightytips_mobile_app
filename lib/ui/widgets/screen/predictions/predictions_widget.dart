import 'package:flutter/material.dart';

class PredictionsWidget extends StatelessWidget {
  const PredictionsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Predictions Screen');
  }
}