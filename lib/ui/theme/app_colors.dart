import 'package:flutter/material.dart';

abstract class AppColors {
  static const bgDark = Color(0xFF1E2223);
  static const bgGray = Color.fromRGBO(4, 44, 53, 1);
  static const blue = Color(0xFF0679B0);
}